version: 2
tag: elephant_kraal
building:
  key: elephant_kraal
  mode: CREATE
  source: pp_new_buildings.txt
  production_method_slots:
    - name: slot_0
      methods:
        - pp_elephant_kraal_base_elephants
    - name: slot_1
      methods:
        - pp_elephant_kraal_no_capture
        - pp_elephant_kraal_kheddah_stockades
  possible_production_methods: []
  body: |2-
    is_foreign = no
    max_levels = farm_capacity_max_elephant_kraal
    pop_type = laborers
    employment_size = 1
    category = rgo_building_category
    custom_tags = { farming_capacity }
    icon = elephant_kraal
    rural_settlement = yes
    town = yes
    city = yes
    megalopolis = yes
    build_time = village_build_time

    location_potential = {
        raw_material = goods:elephants
    }


    construction_demand = basic_construction_needs
    ai_forbid_shutdown = no
    forbidden_for_estates = no
    important_for_AI = no
    can_close = yes

    unique_production_methods = {
        pp_elephant_kraal_base_elephants = {
            produced = elephants
            output = 0.036
            category = building_maintenance
        }
    }
    unique_production_methods = {
        pp_elephant_kraal_no_capture = {
            category = building_maintenance
        }
        pp_elephant_kraal_kheddah_stockades = {
            lumber = 0.103 # stockades, gates
            fruit = 0.171 # fodder, bait
            produced = elephants
            output = 0.076
            category = building_maintenance
        }
    }

    raw_modifier = {
      farm_capacity_from_elephant_kraal = -1
    }

    modifier = {
        local_migration_attraction = 0.003
    }
localization:
  entries:
    elephant_kraal_slot_0: Wild Herds
    elephant_kraal_slot_1: Capture Stockades
    elephant_kraal: Elephant Kraal
    elephant_kraal_desc: Timber kraals hold wild elephants while mahouts calm and train them for work or war. Larger stockades support organized capture drives.
    pp_elephant_kraal_base_elephants: Wild Herds
    pp_elephant_kraal_no_capture: No Capture Drives
    pp_elephant_kraal_kheddah_stockades: Capture Stockades
icon:
  source_png: ../assets/icons/elephant_kraal.png
  output_dds: elephant_kraal.dds
  size: 512
  prompt: EU5-inspired hand-painted strategy-game icon for an Elephant Kraal, transparent background. Center a timber elephant stockade in a tropical forest clearing, one calm elephant partly inside the gate, sturdy wooden posts and rails, a raised mahout platform or training post, and bundles of fruit and fodder near the entrance. Use readable painterly shapes, softly outlined historical UI art, muted jungle greens, warm wood browns, natural gray elephant, no text, no frame, not photorealistic, not cartoonish.

evaluation:
  allow_rules:
    input_throughput: Existing accepted input scale predates the global employment throughput baseline; revisit during the building-specific rebalance.
    output_throughput: Existing accepted output scale predates the global employment throughput baseline; revisit during the building-specific rebalance.
  production_methods:
    pp_elephant_kraal_base_elephants:
      production_efficiency: 1.00
      allow_rules:
        base_output_per_1k: Passive base output intentionally carries 30% of the full worked elephant output after the high-base rebalance.
        amortization_months: Existing accepted elephant kraal payback is faster than the new amortization minimum; revisit during the elephant kraal rebalance.
    pp_elephant_kraal_kheddah_stockades:
      production_efficiency: 1.10
      allow_rules:
        amortization_months: Existing accepted elephant kraal payback is faster than the new amortization minimum; revisit during the elephant kraal rebalance.
