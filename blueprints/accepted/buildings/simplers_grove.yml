version: 2
tag: simplers_grove
building:
  key: simplers_grove
  mode: CREATE
  source: pp_new_buildings.txt
  production_method_slots:
    - name: slot_0
      methods:
        - pp_simplers_grove_base_medicaments
    - name: slot_1
      methods:
        - pp_simplers_grove_no_drying
        - pp_simplers_grove_wicker_drying_racks
  possible_production_methods: []
  body: |2-
    is_foreign = no
    increase_per_level_cost = 0.23
    max_levels = farm_capacity_max_simplers_grove
    pop_type = laborers
    employment_size = 1
    category = rgo_building_category
    custom_tags = { farming_capacity }
    icon = simplers_grove
    rural_settlement = yes
    town = yes
    city = yes
    megalopolis = yes
    build_time = village_build_time

    location_potential = {
        raw_material = goods:medicaments
    }

    construction_demand = basic_construction_needs
    ai_forbid_shutdown = no
    forbidden_for_estates = no
    important_for_AI = no
    can_close = yes

    unique_production_methods = {
        pp_simplers_grove_base_medicaments = {
            produced = medicaments
            output = 0.1
            category = building_maintenance
        }
    }
    unique_production_methods = {
        pp_simplers_grove_no_drying = {
            category = building_maintenance
        }
        pp_simplers_grove_wicker_drying_racks = {
            fiber_crops = 0.035 # baskets, ties, drying screens and mats
            pottery = 0.020 # crocks and dry storage vessels
            produced = medicaments
            output = 0.116
            category = building_maintenance
        }
    }

    raw_modifier = {
      farm_capacity_from_simplers_grove = -1
    }

    modifier = {
        local_migration_attraction = 0.003
    }
localization:
  entries:
    simplers_grove_slot_0: Herb Grounds
    simplers_grove_slot_1: Drying & Sorting
    simplers_grove: Herb Gatherers' Grove
    simplers_grove_desc: Herb gatherers collect useful roots, leaves, flowers, and bark for apothecaries. Drying racks and sorting sheds prepare cleaner medicaments for market.
    pp_simplers_grove_base_medicaments: Herb Grounds
    pp_simplers_grove_no_drying: No Drying Racks
    pp_simplers_grove_wicker_drying_racks: Drying Racks
icon:
  source_png: ../assets/icons/simplers_grove.png
  output_dds: simplers_grove.dds
  size: 512
  prompt: EU5-inspired hand-painted strategy-game icon for a simplers' grove, transparent background. Center herb bundles, roots, small flowers, strips of medicinal bark, wicker baskets, low drying racks, and a small sorting table in a woodland meadow gathering site. Use readable painterly shapes, softly outlined historical UI art, muted herb greens, root browns, bark browns, wicker tan, pale flower accents, no text, no frame, not photorealistic, not cartoonish.

evaluation:
  allow_rules:
    input_throughput: Existing accepted input scale predates the global employment throughput baseline; revisit during the building-specific rebalance.
    output_throughput: Existing accepted output scale predates the global employment throughput baseline; revisit during the building-specific rebalance.
  production_methods:
    pp_simplers_grove_base_medicaments:
      production_efficiency: 1.00
      allow_rules:
        amortization_months: Method payback intentionally sits outside the global building-payback target after error-log profit tuning.
    pp_simplers_grove_wicker_drying_racks:
      production_efficiency: 1.00
      allow_rules:
        amortization_months: Method payback intentionally sits outside the global building-payback target after error-log profit tuning.
