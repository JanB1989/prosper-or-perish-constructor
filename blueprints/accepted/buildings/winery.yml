version: 2
tag: winery
building:
  key: winery
  mode: REPLACE
  source: pp_building_adjustments.txt
  production_method_slots:
  - name: slot_0
    methods:
    - pp_winery_fruit_winery_maintenance
    - pp_winery_rice_winery_maintenance
    - pp_winery_honey_winery_maintenance
  possible_production_methods: []
  body: |
    is_foreign = no
    pop_type = burghers
    max_levels = guild_max_level
    startup_ramp_target = guild_startup_ramp_target
    category = consumer_goods_category
    employment_size = 0.3
    town = yes
    city = yes
    megalopolis = yes
    build_time = guild_build_time
    unique_production_methods = {
        pp_winery_fruit_winery_maintenance = {
            fruit = 1.305
            lumber = 0.178
            tools = 0.104
            produced = wine
            output = 1.0
            category = guild_input
            debug_max_profit = guild_profit_margin
        }
        pp_winery_rice_winery_maintenance = {
            rice = 1.317
            lumber = 0.169
            tools = 0.105
            produced = wine
            output = 1.0
            category = guild_input
            debug_max_profit = guild_profit_margin
        }
        pp_winery_honey_winery_maintenance = {
            beeswax = 0.625
            lumber = 0.208
            tools = 0.105
            produced = wine
            output = 1.0
            category = guild_input
            debug_max_profit = guild_profit_margin
        }
    }
    increase_per_level_cost = 0.25
localization:
  entries:
    winery_slot_0: Winery Work
    pp_winery_fruit_winery_maintenance: Winery Fruit Winery Maintenance
    pp_winery_rice_winery_maintenance: Winery Rice Winery Maintenance
    pp_winery_honey_winery_maintenance: Winery Honey Winery Maintenance
evaluation:
  allow_rules:
    profit_percent: Converted vanilla producer balanced by profit per building against the farming benchmark; throughput and margin bands are advisory here.
    input_throughput: Converted vanilla producer preserves vanilla production shape while targeting profit per building, not throughput per worker.
    output_throughput: Converted vanilla producer preserves vanilla production shape while targeting profit per building, not throughput per worker.
