# Counts staffed victuals export levels: only the export market uses it (1 per level), so its market demand
# equals the export volume and the monthly delivery can read it per market without scanning buildings.
# Cheap by design (floor 0.02 gold) and always in supply.
export_tally = {
	category = produced
	color = goods_export_tally
	default_market_price = 0.1
	transport_cost = 100.0
	base_production = 1000.0
}
