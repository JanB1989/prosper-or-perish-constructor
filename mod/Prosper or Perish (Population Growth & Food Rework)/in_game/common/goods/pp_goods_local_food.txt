local_food = {
	category = produced
	color = goods_local_food
	# Province Food: 1 food per unit, 0.10 gold at the 20 % price floor (default 0.5), the victuals-market rate
	# (3 victuals x 3 gold = 90 food). Markets hold no food, so it never leaves the province that makes it.
	default_market_price = 0.5
	transport_cost = 100.0
	food = 1
	base_production = 1000.0 # huge base supply pins the price at the floor from day one (like offset)
}
