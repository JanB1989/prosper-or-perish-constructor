pp_raise_owned_zero_rgo_max_workers = {
	effect = {
		every_owned_location = {
			limit = {
				exists = raw_material
				rgo_level >= 0
				NOT = { rgo_level >= 1 }
				OR = {
					"unemployed_pops_of_pop_type_in_location(pop_type:laborers)" > 0
					"unemployed_pops_of_pop_type_in_location(pop_type:slaves)" > 0
				}
			}
			change_max_raw_material_workers = 1
		}
	}
}

# Yearly AI building review, AI countries only: one pass over the owned locations that culls one closed building level
# per location and adds victuals import levels where provinces are very short of food (pp_ai_building_review_effects.txt).
pp_yearly_ai_building_review = {
	effect = {
		if = {
			limit = { is_ai = yes }
			pp_ai_building_review_effect = yes
		}
	}
}

pp_ai_logistics_on_unsupported_building_levels = {
	effect = {
		pp_ai_logistics_build_unsupported_infrastructure = yes
	}
}
