# Prosper or Perish - values for the yearly AI building review (pp_ai_building_review_effects.txt)

# Stored food (months) below which a province counts as very short of food for the AI import review.
pp_tavern_low_storage_months = 3

# Country: stored gold the AI can put into construction without hurting itself. Borrowed money does not count and
# half a year of income stays in the bank.
pp_ai_spare_construction_gold = {
	value = gold
	subtract = total_debt
	subtract = { value = monthly_income_total multiply = 6 }
	min = 0
}
